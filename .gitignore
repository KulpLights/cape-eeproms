programmer/vendor-image.dat
programmer/serialNumbers.txt
programmer/programmer-config.json
# A reseller's own serial-number script: their format, their key, their
# database.  Never published, so an update of this tree cannot clobber it.
programmer/*.local

# The programmer build check_for_new.sh fetches from CI for this platform and
# FPP major, the marker recording which asset it is, and its download temp.
programmer/.programmer-installed
programmer/.programmer-major
programmer/.programmer.*.gz
